{
  "name": "@burger-ai/database",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "prisma generate && (prisma db push --accept-data-loss || echo 'Local database push skipped') && tsc",
    "dev": "tsc --watch",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^5.10.2",
    "@supabase/supabase-js": "^2.112.3"
  },
  "devDependencies": {
    "prisma": "^5.10.2",
    "typescript": "^5.3.3"
  }
}
